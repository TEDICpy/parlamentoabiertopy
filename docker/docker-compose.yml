padata:
  build: data/.
  volumes:
    - pasrc:/src
    - painit:/init
popit:
  build: popit/.
  volumes_from:
    - padata
  ports:
    - "8000:8000"
  #links:
  #  - wpdb
#billit:
#  build: billit/.
#  volumes_from:
#    - padata
#  ports:
#    - "8002:8002"
##legislative:
#  build: legislative/.
#  #volumes: 
#    #- $PA_BILLIT_REP:$PA_BILLIT_GEM 
#    #- $PA_POPIT_REP:$PA_POPIT_GEM
#  volumes_from:
#    - padata
#  ports:
#    - "5000:5000"
#
